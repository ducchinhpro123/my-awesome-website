    <nav class="navbar navbar-expand-lg fixed-top bg-red navbar-dark">
        <div class="container">
            <a class="fw-bold d-flex align-items-center" href="index.html" style="line-height: 1;">
                <img src="/assets/images/logo3-1.png" style='width: 100px;'/>
            </a>
            <button data-bs-toggle="collapse" data-bs-target="#navbarNav" class="navbar-toggler" type="button"><span
                        class="navbar-toggler-icon"></span></button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link {% if active_page == 'home' %} active {% endif %}" href="/">Trang Chủ</a></li>
                    <li class="nav-item"><a class="nav-link {% if active_page == 'products' %} active {% endif %}" href="/?action=products">Sản Phẩm</a></li>
                    <li class="nav-item dropdown"><a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#"
                                                     role="button"> Danh Mục </a>
                        <ul class="dropdown-menu">
                            {% for category in categories %}
                                <li><a class="dropdown-item" href="/?action=products-filter&category={{ category.name|url_encode }}">{{ category.name }}</a></li>
                            {% endfor %}
                        </ul>
                    </li>
                    <li class="nav-item"><a class="nav-link {% if active_page == 'introduction' %} active {% endif %}" href="about.html">Giới Thiệu</a></li>
                    <li class="nav-item"><a class="nav-link {% if active_page == 'contact' %} active {% endif %}" href="contact.html">Liên Hệ</a></li>
                </ul>
                <div class="d-flex align-items-center">
                    <form class="d-flex me-3" role="search" method='get' id='search-form'>
                        <input type='hidden' name='action' value='products-filter'/>

                        {% if searchTerm is defined %}
                            <input class="form-control me-2" value="{{ searchTerm }}" type="search" name='query' aria-label="Search" placeholder="Tìm kiếm sản phẩm...">
                        {% else %}
                            <input class="form-control me-2" type="search" name='query' aria-label="Search" placeholder="Tìm kiếm sản phẩm...">
                        {% endif %}
                        <button class="btn btn-outline-light" type="submit"><i class="fas fa-search"></i></button>
                    </form>
                    <a class="btn btn-outline-light me-2" role="button" href="/?action=cart">
                        <i class="fas fa-shopping-cart"></i>
                        <span class="badge rounded-pill text-dark" id="cart-count" style="background-color: var(--golden);">
                            {% if numCartItems is defined %}
                                {{ numCartItems }}
                            {% else %}
                                0
                            {% endif %}
                        </span>
                    </a>

                    {% if username is defined %}
                        <!-- User Dropdown Menu -->
                        <div class="dropdown">
                            <button class="btn btn-outline-light dropdown-toggle d-flex align-items-center" 
                                    type="button" 
                                    id="userDropdown" 
                                    data-bs-toggle="dropdown" 
                                    aria-expanded="false">
                                <i class="fas fa-user-circle me-2"></i>
                                <span class="d-none d-md-inline">Xin chào, {{ username }}</span>
                                <span class="d-md-none">{{ username }}</span>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="userDropdown">
                                {% if isAdmin is defined and isAdmin %}
                                <li>
                                    <a class="dropdown-item text-primary fw-bold" href="/?action=admin">
                                        <i class="fas fa-cog me-2"></i>Admin Dashboard
                                    </a>
                                </li>
                                <li><hr class="dropdown-divider"></li>
                                {% endif %}
                                <li>
                                    <a class="dropdown-item" href="/?action=profile">
                                        <i class="fas fa-user me-2"></i>Thông Tin Cá Nhân
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="/?action=orders">
                                        <i class="fas fa-shopping-bag me-2"></i>Đơn Hàng Của Tôi
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="/?action=wishlist">
                                        <i class="fas fa-heart me-2"></i>Sản Phẩm Yêu Thích
                                    </a>
                                </li>
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                    <a class="dropdown-item text-danger" href="/?action=logout">
                                        <i class="fas fa-sign-out-alt me-2"></i>Đăng Xuất
                                    </a>
                                </li>
                            </ul>
                        </div>
                    {% else %}
                        <a class="btn btn-light" role="button" href="/?action=login">
                            <i class="fas fa-user me-1"></i>Đăng Nhập
                        </a>
                    {% endif %}


                </div>
            </div>
        </div>
    </nav>
